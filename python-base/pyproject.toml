[project]
name = "multithreading-lab-python"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "unidecode",
    "fastapi",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "pytest",
]
